name: Release

# Fires on a version tag. MinVer turns the tag into the assembly version.
on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: windows-latest
    permissions:
      contents: write # required to create the GitHub Release

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # MinVer needs full history to resolve the version from the tag.

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # build.ps1 restores, builds, RUNS THE TESTS, then publishes. Tests run before
      # any artifact is packaged, so a red test blocks the release outright.
      - name: Build, test and publish
        shell: pwsh
        run: .\build.ps1

      # ── Code signing ────────────────────────────────────────────────────────
      # The self-update path executes a downloaded installer AS SYSTEM. An unsigned
      # (or hash-unverified) installer on that path is a remote-code-execution vector:
      # UpdateConfig.RequireSignature + TrustedThumbprint only help if releases are
      # actually signed. This step signs all three executables with a code-signing
      # certificate supplied via repository secrets.
      #
      # It is guarded: if CODE_SIGNING_PFX_BASE64 is absent the step is a clear no-op
      # and prints a warning, so forks/PR builds still produce artifacts — but a real
      # release MUST provide the secret. An unsigned release is a security risk.
      - name: Sign executables
        shell: pwsh
        env:
          PFX_BASE64: ${{ secrets.CODE_SIGNING_PFX_BASE64 }}
          PFX_PASSWORD: ${{ secrets.CODE_SIGNING_PFX_PASSWORD }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:PFX_BASE64)) {
            Write-Warning "CODE_SIGNING_PFX_BASE64 not set — publishing UNSIGNED binaries. The self-update path trusts signatures; unsigned releases are a security risk."
            exit 0
          }

          $pfxPath = Join-Path $env:RUNNER_TEMP "codesign.pfx"
          [IO.File]::WriteAllBytes($pfxPath, [Convert]::FromBase64String($env:PFX_BASE64))

          # Locate signtool from the installed Windows SDK.
          $signtool = Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\bin\*\x64\signtool.exe" |
            Sort-Object FullName -Descending | Select-Object -First 1 -ExpandProperty FullName
          if (-not $signtool) { throw "signtool.exe not found on the runner." }

          $targets = @(
            "publish/Installer/KidControl.Installer.exe",
            "publish/ServiceHost/KidControl.ServiceHost.exe",
            "publish/UiHost/KidControl.UiHost.exe"
          )
          foreach ($t in $targets) {
            & $signtool sign /f $pfxPath /p $env:PFX_PASSWORD `
              /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 $t
            if ($LASTEXITCODE -ne 0) { throw "signtool failed for $t" }
          }

          Remove-Item $pfxPath -Force

      # ── Checksums ───────────────────────────────────────────────────────────
      # Computed AFTER signing so the hashes match exactly what users download.
      - name: Generate SHA256SUMS.txt
        shell: pwsh
        run: |
          $files = @(
            "publish/Installer/KidControl.Installer.exe",
            "publish/ServiceHost/KidControl.ServiceHost.exe",
            "publish/UiHost/KidControl.UiHost.exe"
          )
          $lines = foreach ($f in $files) {
            $hash = (Get-FileHash $f -Algorithm SHA256).Hash.ToLower()
            "$hash  $([IO.Path]::GetFileName($f))"
          }
          $lines | Set-Content -Path "publish/SHA256SUMS.txt" -Encoding ascii
          Get-Content "publish/SHA256SUMS.txt"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            publish/Installer/KidControl.Installer.exe
            publish/ServiceHost/KidControl.ServiceHost.exe
            publish/UiHost/KidControl.UiHost.exe
            publish/SHA256SUMS.txt
