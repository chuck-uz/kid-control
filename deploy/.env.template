# Render this to `.env` from Infisical (do NOT commit the real .env).
# The compose file reads these.

POSTGRES_DB=kidcontrol
POSTGRES_USER=kidcontrol
POSTGRES_PASSWORD=change-me            # required

# Operator's Telegram chat id — seeded as the first admin on boot (T3). Leave empty to skip.
FLEET_ADMIN_CHAT_ID=

# Telegram bot token for the in-backend bot (added in a later task).
TELEGRAM_BOT_TOKEN=
